#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/wid?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#    #    mysql에 생성한 사용자 계정 정보를 써야 합니다.
#    username: widAdmin
#    password: W3qEmYxj
#  thymeleaf:
#    cache: false

spring:
  # H2 Setting Info (H2 Console에 접속하기 위한 설정정보 입력)
  h2:
    console:
      enabled: true  # H2 Console을 사용할지 여부 (H2 Console은 H2 Database를 UI로 제공해주는 기능)
      path: /h2-console  # H2 Console의 Path
  # Database Setting Info (Database를 H2로 사용하기 위해 H2연결 정보 입력)
  datasource:
    driver-class-name: org.h2.Driver  # Database를 H2로 사용하겠다.
    url: jdbc:h2:~/test  # H2 접속 정보
    username: sa  # H2 접속 시 입력할 username 정보 (원하는 것으로 입력)
    password:  # H2 접속 시 입력할 password 정보 (원하는 것으로 입력)
  # spring data jpa 설정
  jpa:
    #    database-platform: org.hibernate.dialect.MySQL8Dialect
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update

jwt:
  secret: 256268299e20811587573c95713c687a9b9906bbc0b2f9ae2fc9ef99352c481e238b74fc0992116b9495c7485eb4155167cea440c1e4f252478deeffaeecaa2e